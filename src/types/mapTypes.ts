export type EpisodeIdMapItem = {
  name: string;
  seasonId: string;
};

export type SeasonIdMapItem = string;

export type SpeakerIdMapItem = {
  name: string;
  imageUrl: string;
  color: string;
};
